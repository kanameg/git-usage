services:
  git-usage: # service nameで、container_nameと合わせる
    build:
      context: .
      dockerfile: Dockerfile
    container_name: git-usage
    expose: 
      - 3000
    stdin_open: true
    tty: true
    working_dir: /workspace
    volumes:
      - ../:/workspace
